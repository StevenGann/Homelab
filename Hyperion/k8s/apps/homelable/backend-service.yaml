apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: homelable
spec:
  type: ClusterIP
  selector: { app: homelable-backend }
  ports:
    - { name: http, port: 8000, targetPort: 8000 }
