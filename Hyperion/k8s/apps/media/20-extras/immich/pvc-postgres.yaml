apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-postgres-data
  namespace: media
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path        # node-local; PostgreSQL must NOT run over NFS
  resources:
    requests:
      storage: 50Gi
