apiVersion: v1
kind: Service
metadata:
  name: sharedirstat
  namespace: sharedirstat
  annotations: { metallb.io/loadBalancerIPs: 192.168.10.92 }
spec:
  type: LoadBalancer
  selector: { app: sharedirstat }
  ports:
    - { name: http, port: 80, targetPort: 8080 }
