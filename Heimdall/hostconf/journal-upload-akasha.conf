# Heimdall — systemd-journal-upload drop-in.
#
# Installed to /etc/systemd/journal-upload.conf.d/akasha.conf by setup.sh.
# Mirrors the Hyperion pattern: ship all journal entries (including Docker journald
# log driver output for every container) to Akasha's journal-remote receiver.
#
# Akasha side: docker-compose service `journal-remote` listening on :19532.
# Persistent local journal kept (under /var/log/journal) so we have local buffering
# when Akasha is unreachable.

[Upload]
URL=http://192.168.10.247:19532
