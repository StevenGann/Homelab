apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: suggestarr-config
  namespace: media
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path        # node-local; wizard-written config persists here
  resources: { requests: { storage: 1Gi } }
