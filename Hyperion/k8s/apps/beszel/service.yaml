apiVersion: v1
kind: Service
metadata:
  name: beszel
  namespace: beszel
  annotations: { metallb.io/loadBalancerIPs: 192.168.10.68 }
spec:
  type: LoadBalancer
  selector: { app: beszel }
  ports: [ { name: http, port: 8090, targetPort: 8090 } ]
