apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homarr-appdata
  namespace: media
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path        # node-local; embedded SQLite must NOT run over NFS
  resources: { requests: { storage: 1Gi } }
