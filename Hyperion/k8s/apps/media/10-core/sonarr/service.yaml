apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: media
  annotations:
    metallb.io/loadBalancerIPs: 192.168.10.56
spec:
  type: LoadBalancer
  selector: { app: sonarr }
  ports:
    - { name: http, port: 8989, targetPort: 8989 }
