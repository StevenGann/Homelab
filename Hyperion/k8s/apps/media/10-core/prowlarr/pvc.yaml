apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-config
  namespace: media
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path        # node-local; SQLite must NOT run over NFS
  resources: { requests: { storage: 1Gi } }
