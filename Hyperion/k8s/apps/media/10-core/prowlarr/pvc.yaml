# prowlarr-config on Akasha NFS (Application-Storage), NOT node-local.
# 2026-06-05 storage migration (docs/storage-audit-2026-06-05.md): no Hyperion
# service keeps persistent data on a node's local NVMe. Prowlarr SQLite. SQLite/embedded
# DBs work over NFSv4.1 'hard' mounts with locking. Static PV+PVC per the
# akasha-*-nfs convention in media/00-storage/. PVC name unchanged.
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: akasha-app-media-prowlarr-config
  annotations: { kustomize.toolkit.fluxcd.io/prune: disabled }
spec:
  capacity: { storage: 1Gi }
  accessModes: [ReadWriteMany]
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  volumeMode: Filesystem
  claimRef: { namespace: media, name: prowlarr-config }
  mountOptions: [nfsvers=4.1, hard, noatime]
  nfs: { server: 192.168.10.247, path: /mnt/Media-Storage/Application-Storage/media/prowlarr-config }
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-config
  namespace: media
  annotations: { kustomize.toolkit.fluxcd.io/prune: disabled }
spec:
  accessModes: [ReadWriteMany]
  storageClassName: ""
  volumeName: akasha-app-media-prowlarr-config
  resources: { requests: { storage: 1Gi } }
