apiVersion: v1
kind: Service
metadata:
  name: musicseerr
  namespace: musicseerr
  annotations: { metallb.io/loadBalancerIPs: 192.168.10.74 }
spec:
  type: LoadBalancer
  selector: { app: musicseerr }
  ports:
    - { name: http, port: 8688, targetPort: 8688 }
    - { name: web,  port: 80,   targetPort: 8688 }
