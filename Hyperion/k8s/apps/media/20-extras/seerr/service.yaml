apiVersion: v1
kind: Service
metadata:
  name: seerr
  namespace: media
  annotations:
    metallb.io/loadBalancerIPs: 192.168.10.54
spec:
  type: LoadBalancer
  selector: { app: seerr }
  ports:
    - { name: http, port: 5055, targetPort: 5055 }
