apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cassandra-data
  namespace: cassandra
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
