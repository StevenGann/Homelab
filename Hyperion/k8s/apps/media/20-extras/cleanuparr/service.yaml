apiVersion: v1
kind: Service
metadata:
  name: cleanuparr
  namespace: media
  annotations:
    metallb.io/loadBalancerIPs: 192.168.10.59
spec:
  type: LoadBalancer
  selector: { app: cleanuparr }
  ports:
    - { name: http, port: 11011, targetPort: 11011 }
    - { name: web,  port: 80,    targetPort: 11011 }   # bare http://cleanuparr.lab
