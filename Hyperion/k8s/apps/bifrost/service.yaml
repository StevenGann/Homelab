apiVersion: v1
kind: Service
metadata:
  name: bifrost
  namespace: bifrost
  annotations: { metallb.io/loadBalancerIPs: 192.168.10.20 }
spec:
  type: LoadBalancer
  selector: { app: bifrost }
  ports:
    - { name: http, port: 11434, targetPort: 11434 }
