apiVersion: v1
kind: Service
metadata:
  name: jellyfin
  namespace: media
  annotations: { metallb.io/loadBalancerIPs: 192.168.10.70 }
spec:
  type: LoadBalancer
  selector: { app: jellyfin }
  ports: [ { name: http, port: 8096, targetPort: 8096 } ]
