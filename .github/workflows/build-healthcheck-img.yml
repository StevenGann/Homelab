name: Build and publish healthcheck image

on:
  push:
    branches: [main]
    paths:
      - 'Monolith/k3s-control-plane/healthcheck/**'
  workflow_dispatch:

concurrency:
  group: build-healthcheck
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push healthcheck image
        uses: docker/build-push-action@v5
        with:
          context: Monolith/k3s-control-plane/healthcheck
          push: true
          tags: ghcr.io/stevengann/homelab-healthcheck:latest
