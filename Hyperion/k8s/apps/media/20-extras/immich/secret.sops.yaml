apiVersion: v1
kind: Secret
metadata:
  name: immich-secret
  namespace: media
type: Opaque
stringData:
  # SOPS-encrypt this file before committing:
  #   SOPS_AGE_KEY_FILE=~/.config/sops/age/keys.txt sops --encrypt --in-place secret.sops.yaml
  DB_PASSWORD: immich_db_pass_2026
