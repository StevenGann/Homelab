name: Build and publish ci-deploy image

on:
  push:
    branches: [main]
    paths:
      - 'Monolith/k3s-control-plane/ci-deploy/**'
  workflow_dispatch:

concurrency:
  group: build-ci-deploy
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ci-deploy image
        uses: docker/build-push-action@v5
        with:
          context: Monolith/k3s-control-plane/ci-deploy
          push: true
          tags: ghcr.io/stevengann/homelab-ci-deploy:latest
