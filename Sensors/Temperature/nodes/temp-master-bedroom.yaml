# Per-node config. All logic lives in the shared package.
packages:
  base: !include ../common/dht22-node.yaml

substitutions:
  node_name: temp-master-bedroom
  friendly_name: "Master Bedroom"
  # dht_pin (D5) and update_interval (60s) inherit package defaults unless set here.
