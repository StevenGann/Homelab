apiVersion: v1
kind: PersistentVolumeClaim
metadata: { name: sharedirstat-data, namespace: sharedirstat }
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources: { requests: { storage: 1Gi } }
