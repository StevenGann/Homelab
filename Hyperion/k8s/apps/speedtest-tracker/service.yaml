apiVersion: v1
kind: Service
metadata:
  name: speedtest-tracker
  namespace: speedtest-tracker
  annotations: { metallb.io/loadBalancerIPs: 192.168.10.67 }
spec:
  type: LoadBalancer
  selector: { app: speedtest-tracker }
  ports: [ { name: http, port: 80, targetPort: 80 } ]
