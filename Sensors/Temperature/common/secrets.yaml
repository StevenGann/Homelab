# Relay file — holds NO secret values.
# ESPHome resolves `!secret` relative to the directory of the file containing the
# tag. The shared package lives in common/, so it needs a secrets.yaml here; this
# one simply re-includes the single real secrets.yaml at the project root.
# (Committed on purpose — it contains no credentials.)
<<: !include ../secrets.yaml
